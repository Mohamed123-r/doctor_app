import 'package:flutter/material.dart';

class MedicalRecordsBody extends StatelessWidget {
  const MedicalRecordsBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Medical Records Body'),
    );
  }
}